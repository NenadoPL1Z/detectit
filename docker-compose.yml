services:
  prod:
    image: detectit/frontend/production
    container_name: detectit/frontend/production
    restart: always
    build:
      context: .
      target: builder
    ports:
      - "3000:3000"
    command: serve -s dist -l 3000
