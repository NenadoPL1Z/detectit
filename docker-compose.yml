services:
  detectit:
    image: detectit
    container_name: detectit
    tty: true
    restart: always
    stdin_open: true
    ports:
      - "3000:3000"
    build:
      context: .
      dockerfile: Dockerfile
