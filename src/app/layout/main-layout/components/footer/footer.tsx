import { Typography } from "@shared/ui";

export const Footer = () => {
  return (
    <footer>
      <Typography variant="m700" fontSize={12}>
        footer
      </Typography>
    </footer>
  );
};
